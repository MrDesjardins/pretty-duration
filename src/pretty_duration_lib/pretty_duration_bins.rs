#[derive(Copy, Clone)]
pub struct DurationBins {
    pub years: u16,
    pub months: u8,
    pub days: u8,
    pub hours: u8,
    pub minutes: u8,
    pub seconds: u8,
    pub milliseconds:u16,
}
